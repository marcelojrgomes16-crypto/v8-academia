# Aprendizados

> Tudo que eu aprendi.

## Next.js

- `export const dynamic = 'force-dynamic'` para server-side
- `useRouter()` do next/navigation
- Params e Promise: `const { id } = await params`

## React Native / Expo

- SEM EMOJIS (usar ASCII)
- Inputs: defaultValue + onBlur
- RefreshControl: onScrollEndDrag no web

## Prisma

- `@@index()` cria indices
- `include` faz join
- `select` escolhe campos

## Tailwind CSS

- `dark:` para tema escuro
- `hover:` para hover
- `sm:`, `md:`, `lg:` para responsivo

## Git

- Commitar frequentemente
- Nao committar .env

## Seguranca

- NUNCA expor credenciais
- Usar variaveis de ambiente
